{
  "name": "cypress_workspace",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "spec": "npx cypress run --spec **/*.spec.js",
    "postspec": "node Test.js --continue-on-error",
    "all":"npx cypress run --spec **/**/*.spec.js --headed",
    "LoginTest":"npx cypress run --spec **/MyAccount/*.spec.js --headed"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^8.7.0",
    "cypress-mochawesome-reporter": "^2.3.0"
  },
  "dependencies": {
    "fs-extra": "^10.0.0"
  }
}
